---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: mikefarah/yq

inputs:
  - name: source-base-code
  - name: cd-resources

outputs:
- name: target-resources

run:
  path: sh 
  args:
  - cd-resources/scripts/update-deployment-image.sh
  - source-base-code/test.yaml
  - karankapoor/multi-tenant:((imageVersion))
